# settings
# each can be also passed as Environment variable with same name like: basic_auth=true rackup ...
basic_auth:    false
username:      admin
password:      admin

hooks_to_store: 50
hooks_storage:  ram